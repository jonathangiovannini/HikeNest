export interface TeamMember {
    name: string;
    role: string;
    description: string;
    image: string;
    roleColor: string;
}
export interface Mission {
    icon: string;
    title: string;
    description: string;
    color: string;
}